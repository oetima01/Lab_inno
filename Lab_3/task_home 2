#include <stdio.h>
#include <string.h>


int sp[1000], k = 0;


int find (int p){
    for (int j = 0; j < k; j++){
        if (sp[j] == p) {
            return 0;
        }
    }
    sp[k] = p;
    k++;
    return 1;
}



int main(void) {
    //Set(
    int n, q, rab;
    scanf("%d", &n);
    for (int i = 0; i < n; i++){
        scanf("%d", &q);
        rab = find(q);
        if (rab == 1){
            printf("%d ", q);
        }
    }
    return 0;
}
