#include <stdio.h>
#include <string.h>
#define mx 100


void input(int sp[mx][mx], int n, int m){
    int (*k)[mx] = sp;
    for (int i = 0; i < n; i++){
        for (int j = 0; j < m; j++){
            scanf("%d", &(*(*(k + i) + j)));
        }
    }
}


void print(int sp[mx][mx], int n, int m){
    int (*k)[mx] = sp;
    for (int i = 0; i < n; i++){
        for (int j = 0; j < m; j++){
            printf("%d ", (*(*(k + i) + j)));
        }
        printf("\n");
    }
}


int main(void) {
    //Set(
    int n, m;
    int sp[mx][mx];
    scanf("%d %d", &n, &m);
    input(sp, n, m);
    print(sp, n, m);
    return 0;
}
